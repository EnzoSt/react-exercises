Can you use the `Hello` component more than once in the `App` component? What happens if you do?

Yes, we can use a component as many times as we want, it will be rendered all the times it will be passed.
